import { useEffect,useState } from 'react';import { api } from '../../api.js';import { useAuth } from '../../context/AuthContext.jsx';import { AdminHeader } from './AdminLayout.jsx';
export default function AdminUsersPage(){const {token,user}=useAuth();const [items,setItems]=useState([]);const load=()=>api.adminUsers(token).then(x=>setItems(x.users||[]));useEffect(load,[token]);async function role(id,value){await api.adminSetUserRole(id,value,token);load()}return <><AdminHeader title="Utilisateurs" subtitle="Gérez les rôles et les accès à StudyLink"/><div className="admin-table-wrap"><table className="admin-table"><thead><tr><th>Utilisateur</th><th>Email</th><th>Rôle</th><th>Inscription</th></tr></thead><tbody>{items.map(x=><tr><td><b>{x.full_name}</b>{x.id===user.id&&<small>Votre compte</small>}</td><td>{x.email}</td><td><select disabled={x.id===user.id} value={x.role} onChange={e=>role(x.id,e.target.value)}><option value="student">Apprenant</option><option value="tutor">Tuteur</option><option value="admin">Administrateur</option></select></td><td>{new Date(x.created_at).toLocaleDateString('fr-FR')}</td></tr>)}</tbody></table></div></>}
