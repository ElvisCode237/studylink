import { NavLink, Link, Outlet } from 'react-router-dom';
import { useAuth } from '../../context/AuthContext.jsx';
const links=[['/admin','▦','Tableau de bord'],['/admin/courses','▤','Cours'],['/admin/tutorials','▶','Tutoriels'],['/admin/books','▥','Livres'],['/admin/bootcamps','⚑','Bootcamps'],['/admin/personal-development','✦','Développement perso'],['/admin/entrepreneurship','↗','Entrepreneuriat'],['/admin/users','◎','Utilisateurs']];
export default function AdminLayout(){const {user,logout}=useAuth();return <div className="admin-shell"><aside className="admin-sidebar"><Link to="/" className="admin-brand">◆ <span>StudyLink</span><small>ADMIN</small></Link><nav>{links.map(([to,icon,label])=><NavLink end={to==='/admin'} key={to} to={to} className={({isActive})=>isActive?'admin-link active':'admin-link'}><span>{icon}</span>{label}</NavLink>)}</nav><div className="admin-account"><b>{user?.full_name}</b><small>{user?.email}</small><Link to="/">Voir l’application</Link><button onClick={logout}>Déconnexion</button></div></aside><main className="admin-main"><Outlet/></main></div>}
export function AdminHeader({title,subtitle,action}){return <header className="admin-header"><div><h1>{title}</h1>{subtitle&&<p>{subtitle}</p>}</div>{action}</header>}
export function Status({value}){const map={published:'Publié',draft:'Brouillon',review:'En validation',archived:'Archivé',project:'En projet',upcoming:'À venir',ongoing:'En cours',completed:'Terminé'};return <span className={`admin-status s-${value}`}>{map[value]||value}</span>}
